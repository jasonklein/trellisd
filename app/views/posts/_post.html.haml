%div{class: "profile_post_box #{classname_for_post_box(post)}"}
  .profile_post_box_content
    %h6
      %em
        = display_directionality_if_directionality_cateogry(post)
    %h4
      = post.title
    %h6
      = display_matches_count_if_current_user_post(post)
      / - expiration_class = expiration_class_for_post_box(post)
      %em
        %div{class: expiration_class_for_post_box(post)}
          = expiration_message(post)
    %br
    %h6.text_highlight
      = post.list_keyword_titles
  .profile_post_box_icons
    = display_category_icon(post, '60px')
    / = display_attachment_icon(post)
  .profile_post_box_buttons
    .not_delete_buttons
      = button_to "View", user_post_path(@user, post), method: :get
      - if @user == current_user
        = button_to "Edit", edit_user_post_path(@user, post), method: :get
        .delete_button
          = button_to "Delete", destroy_post_path(@user, post), method: :delete